{
  "name": "library-api",
  "version": "1.0.0",
  "description": "Simple Library API for CODTECH Internship",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}